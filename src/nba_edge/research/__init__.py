"""Research scripts: calibration of simulator constants, walk-forward baselines, market calibration.
Each script reads the historical dataset (data/history) and writes a report under docs/research/. Nothing here
feeds production automatically — findings must be promoted deliberately."""
